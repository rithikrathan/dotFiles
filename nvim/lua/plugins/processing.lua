return {
	"sophieforrest/processing.nvim",
	ft = { "processing" }, -- Load only for .pde files
	-- config = function()
	-- 	require("processing").setup({
	-- 		-- Optional: Enable Treesitter highlighting if you have it installed
	-- 		highlight = {
	-- 			enable = true,
	-- 		},
	-- 		-- Optional: processing-lsp integration (requires manual installation of the LSP)
	-- 		lsp = {
	-- 			cmd = nil,
	-- 		},
	-- 	})
	-- end,
}
